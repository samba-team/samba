#!/bin/sh
# $Id$
if test $# -ne 1; then
  echo "Usage: $0 host"
  exit 1
fi
host=$1
bindir=%bindir%
set foo `$bindir/kx $host`
if test $# -ne 4; then
  exit 1
fi
pid=$2
disp=:$3
auth=$4
kill -USR1 $pid
$bindir/rsh -n $host "env DISPLAY=$disp XAUTHORITY=$auth xterm -T $host -n $host &"
