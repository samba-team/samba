#!/bin/sh
# $Id$

if [ ! -f configure.in ]; then
	echo "Must be in top directory"
	exit 1
fi

if [ $# -ne 1 -a $# -ne 2 ]; then
	echo "make-release version [branch]"
	exit 1
fi

if [ "$2" != "" ]; then
	rev="-r $2"
fi

HV="$1" emacs -batch -q -no-site-file -l ./make-release.el || exit

echo cvs ci -m \"Release $1\" configure.in ChangeLog
cvs ci -m "Release $1" configure.in ChangeLog
v=`echo $1 | sed 's/\(.\)\.\(.\)\(.\)/\1-\2-\3/'`
echo cvs rtag $rev heimdal-$v heimdal
cvs rtag $rev heimdal-$v heimdal
