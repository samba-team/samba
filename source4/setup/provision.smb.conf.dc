[globals]
	netbios name	= ${HOSTNAME}
	workgroup	= ${DOMAIN_CONF}
	realm		= ${REALM_CONF}
	server role     = ${SERVERROLE}

[netlogon]
	path = ${NETLOGONPATH}
	read only = no

[sysvol]
	path = ${SYSVOLPATH}
	read only = no
