[globals]
	netbios name	= ${NETBIOS_NAME}
	workgroup	= ${DOMAIN}
	realm		= ${REALM}
	server role     = ${SERVERROLE}
	${SETUPDIRECTORY_LINE}
	${SIDGENERATOR_LINE}
	${PRIVATEDIR_LINE}
	${LOCKDIR_LINE}
	${POSIXEADB_LINE}

[netlogon]
	path = ${NETLOGONPATH}
	read only = no

[sysvol]
	path = ${SYSVOLPATH}
	read only = no
