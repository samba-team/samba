[globals]
	netbios name	= ${HOSTNAME}
	workgroup	= ${DOMAIN_CONF}
	realm		= ${REALM_CONF}
	server role     = ${SERVERROLE}
	${PRIVATEDIR_LINE}
	${LOCKDIR_LINE}

[netlogon]
	path = ${NETLOGONPATH}
	read only = no

[sysvol]
	path = ${SYSVOLPATH}
	read only = no
