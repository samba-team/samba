[libdefaults]
	default_realm = EXAMPLE.ORG

[realms]
	EXAMPLE.ORG = {
		kdc = localhost
	}

[kdc]
	database = {
		dbname = @objdir@/current-db
		realm = EXAMPLE.ORG
		mkey_file = @objdir@/mkey.file
	}
